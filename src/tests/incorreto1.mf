int main(){

   int count;
   int temp;
   int i;
   int j;
   int j;
   int number;


   write("How many numbers are u going to enter?: ");
   read("%d");

   write("Enter %d numbers: ");

   for(i = 0 i < count; i = i + 1)
    read("%d");

   for(i = count - 2; i > 0; i = i - 1){
      for(j = 0; j <= i; j = j + 1){
        if(number>number){
           %temp=number;
           number=number;
           number=temp;
        }
      }
   }

   write("Sorted elements: ");
   for(i = 0;i < count;i = i + 1)
      write(" %d");

   return 0;
}