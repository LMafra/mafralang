int main(){

    
    
    (1 + 2) - b



    if(f( (1 + 2) - b, 3 / (((3))), exists(b in add(1 in add(2 in s)))) && 2 > 3 || 4){
        // 10
        write("puts");
    }
    // 9
    if( (1) + (2) ){
        // 11
        writeln("eita");
        x = is_set(b);
    }
    // 9
}